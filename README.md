# ChromeDino-Bot

A simple bot 
Packages used:

``pyautogui``

``pywin32``

# Installing the dependancies
`pip install pywin32`
`pip install keyboard`
`pip install pyautogui`
`pip install opencv-python`
